- [x] PipelineBuilder.build() 返回 pipeline.py 的完整 Pipeline 实例
- [x] builder.py 中的简单 Pipeline dataclass 已删除
- [x] Phase 1-4 构造函数只接受 context + 特有参数，不再有 writer/reader/progress_tracker/domain_cache 独立参数
- [x] 不传 context 时 Phase 构造函数报错
- [x] Phase 内不再有 channel.disabled 检查逻辑，统一由 run_concurrent 处理
- [x] BatchTagger._read_channel_data() 使用 self._reader.get_channel_data() 而非鸭子类型
- [x] flush_progress() 公共函数存在于 utils/progress.py，三处 _try_flush 已替换
- [x] InMemoryIPWriter 不再有 get_ip_data/get_channel_data/list_all_ips/list_ip_channels 读取方法
- [x] PipelineBuilder 支持 with_filter() 注册阶段间过滤器
- [x] run_pipeline.py 使用 PipelineBuilder 组装，不再手动创建 Phase
- [x] 882+ 测试全部通过，无回归（892 passed）

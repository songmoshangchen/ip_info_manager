- [x] BatchResult 数据类包含 success_count, fail_count, total_elapsed, stopped_early, stop_reason 五个字段，默认值正确
- [x] ProgressTracker 协议可通过 isinstance 检查
- [x] InMemoryProgressTracker 实现 mark_processed + is_processed
- [x] FileProgressTracker 实现持久化到文件，文件不存在时所有 IP 未处理
- [x] BaseBatchQuery 构造函数接受 channel_name, channel, writer, ips 四个必需参数
- [x] BaseBatchQuery 构造函数接受 delay, no_validate, progress_tracker, max_consecutive_network_failures 四个可选参数
- [x] IP 列表去重：传入重复 IP 时内部自动去重
- [x] total_count 属性返回去重后 IP 总数
- [x] pending_count 属性返回排除已处理后剩余 IP 数
- [x] progress_tracker=None 时处理全部 IP
- [x] 有 progress_tracker 时排除已处理 IP
- [x] 成功查询后调用 progress_tracker.mark_processed(ip)
- [x] ChannelError 后不调用 progress_tracker.mark_processed(ip)（不写入 store，下次可重试）
- [x] ChannelPermanentError 后不调用 progress_tracker.mark_processed(ip)（渠道已 disabled）
- [x] run() 循环通过 channel.fetch(ip, delay=X) 传递延迟
- [x] 成功查询结果写入 store，使用正确的 channel_name 和 data
- [x] ChannelError 不写入 store，计为 failure，增加熔断计数，通过日志记录
- [x] ChannelPermanentError 导致 channel.disabled=True，立即终止查询，返回 stopped_early=True, stop_reason="permanent_error"
- [x] 渠道在运行中被 disabled 后，后续所有 IP 均被跳过
- [x] 非 ChannelError 异常向上传播
- [x] 连续 N 次 ChannelError 触发熔断，返回 stopped_early=True, stop_reason="circuit_break"
- [x] 成功查询重置熔断计数器
- [x] channel.disabled=True 时跳过所有查询
- [x] no_validate=False 时调用 channel.validate()
- [x] no_validate=True 时跳过 channel.validate()
- [x] run() 返回正确填充的 BatchResult
- [x] 无 sys.path.insert hack
- [x] 无 from legacy import ...
- [x] `python -m pytest tests/unit/ -v` 全部通过（含原有 307 测试 + 新测试）
- [x] `ruff check src/ tests/unit/` 无错误

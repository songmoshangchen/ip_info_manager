- [ ] ChannelConfig 基类基于 pydantic-settings，env_prefix="IP_"，从 .env 读取
- [ ] 环境变量覆盖 .env 配置
- [ ] .env 不存在时使用 Field 默认值
- [ ] 必填字段缺失时抛 ValidationError
- [ ] 10 个渠道配置类字段与 legacy config.py 一致
- [ ] BaseChannelAdapter.default_delay 类属性，默认值 0
- [ ] 子类可覆盖 default_delay
- [ ] 各 ChannelAdapter 构造函数支持 config 参数
- [ ] 显式参数 > 配置类 > 代码默认值 优先级正确
- [ ] 无 config 参数时自动创建配置类实例
- [ ] 原有 ChannelAdapter 测试全部通过
- [ ] `python -m pytest tests/unit/ -v` 全部通过
- [ ] `ruff check src/ tests/unit/` 无错误

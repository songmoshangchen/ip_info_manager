- [x] pyproject.toml 存在且 `pip install -e .` 成功，`import ip_info` 可用
- [x] Task 1 使用了 `brainstorming` skill 确认基础决策
- [x] Task 1 使用了 `setup-pre-commit` skill 配置 Python pre-commit hooks（ruff + pytest，排除 legacy/）
- [x] `IPDataWriter` Protocol 已定义且 `@runtime_checkable`，isinstance 检查通过
- [x] `IPDataReader` Protocol 已定义且 `@runtime_checkable`（含 7 个方法），isinstance 检查通过
- [x] `InMemoryIPWriter` 实现了 `IPDataWriter` Protocol，所有写入/删除场景测试通过
- [x] `InMemoryIPReader` 实现了 `IPDataReader` Protocol，所有读取/搜索场景测试通过
- [x] `get_ips_data(ips)` 批量获取接口正常工作，不存在的 IP 不出现在结果中
- [x] `list_all_ips_data(exclude_ips)` 排除接口正常工作
- [x] `InMemoryIPWriter` 同时实现 `IPDataReader` 接口（含批量查询），读写一致性测试通过
- [x] 异常边界行为正确（不存在的 IP/渠道返回 False/None/空列表）
- [x] `IPWriter`（JSON 文件）实现 `IPDataWriter` Protocol，文件 I/O 测试通过
- [x] `IPReader`（JSON 文件）实现 `IPDataReader` Protocol（含批量查询），文件读取测试通过
- [x] `IPWriter` IO 异常透传正确（PermissionError 等直接抛出，不吞异常）
- [x] `IPReader` 文件不存在时返回空数据（非异常）
- [x] `IPWriter` 线程安全，并发写入无数据丢失
- [x] 每个 Task 都使用了 `tdd` skill（RED → GREEN → REFACTOR 循环）
- [x] 每个 Task 完成后都使用了 `git-commit` skill（commit message 含中文翻译）
- [x] 所有新测试文件位于 `tests/unit/store/`，不依赖 `legacy/` 代码
- [x] 无 `sys.path.insert` hack
- [x] 无 `from legacy import ...`
- [x] `python -m pytest tests/unit/store/ -q` 全部通过（87 passed）
